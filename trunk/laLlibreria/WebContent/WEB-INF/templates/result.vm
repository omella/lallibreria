## Velocity Template.
<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>
    <head>
        <title>Resultats per ${key}</title>		  
	</head>
<body>
<div class="search">

	#sform ("action=searchBook")
		#stextfield ("label=Search" "name=key")
		#ssubmit ("value=Search")
    #end
    
	#if ($msg)
		<font color="red">${numberResults} ${msg}</font>
	#end
</div>
#if ($results)
	Aquesta es la pagina ${page} de ${totalPages} pagines de resultats

	<div class="content">
		<table border="1">
		#foreach ($result in $results)
		<tr>
			<td>
				<h1><a href=/laLlibreria/showBook.action?id=${result.isbn}> ${result.titol}</a></h1>
				<h2>${result.autor}</h2>
				<br>
			</td>
		</tr>
		#end
		</table>
	</div>
#else
	<h1> No hi ha resultats </h1>
#end
<p>
<a href=/laLlibreria>Inici</a> 
#if ($results)
	#if ($page > 1)
		<a href=/laLlibreria/previousSearch.action?key=${key}>Previous</a>
	#end
	#if ($page < $totalPages)
		<a href=/laLlibreria/nextSearch.action?key=${key}>Next</a>
	#end
#end
</body>
</html>